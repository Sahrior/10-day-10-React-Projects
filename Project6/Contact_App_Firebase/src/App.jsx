import { ref, push } from "firebase/database";
import { db } from "./config/firebase";

function App() {

  /*const addContact = () => {
    push(ref(db, "contacts"), {
      name: "Sahrior",
      phone: "01700000000"
    });
  };*/

  return (
    <div>
    
    </div>
  );
}

export default App;